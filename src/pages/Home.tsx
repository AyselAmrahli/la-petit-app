import { Typography } from '@material-ui/core';
import React, { FC } from 'react';


const Home: FC = () => {
  return <Typography component="h5" variant="h5">Home Page</Typography>;
};

export default Home;
